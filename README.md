# ai-factory-status

Public BIGFIT Factory status / webmap (GitHub Pages).

- Live: https://liboriodsantos.github.io/ai-factory-status/
- Pointer: [`BRIDGE_STATE.md`](BRIDGE_STATE.md)
- Map: [`AI_FACTORY_MAP.md`](AI_FACTORY_MAP.md)
- Self-build: [`SELF_BUILD_STATUS.md`](SELF_BUILD_STATUS.md)

This repo is the public board, not the Factory software. Factory OS items **37–42** are recorded on the board (11–36 still earned; **33** and **36** remain in progress). Standing host redeploy (**37**, earlier ~`53512c5`) keeps auth + `HTTP_BRIDGE` and stays **PAUSED**; item **43** is a later redeploy and is **not claimed**. Pages 31–36 already live (**38**, this repo PR #15 @ `9d91184`). Discover family shells **DONE** (**39**, factory-bridge **PR #81**). Evidence & Receipts ledger **DONE** (**40**, **#82** @ `ca5fbb0`). Integrations shell **DONE** (**41**, **#83** @ `0d7c7ca`). Home next-best-action **DONE** (**42**, **#84** @ `eb6fe90`). Shells/ledger/NBA are thin — not full journeys. Software PR numbers **#81–#84** and merge SHAs are from Daniel’s item-44 brief (`factory-bridge` 404 from this token). `productionWrites` remains **false**; `FACTORY_MODE` remains **PAUSED**. Write class `factory.self_build.supervised` is named only — design doc, **no flip**. Director keys stay **off host**. Ellisbrook live attach is **NO-GO**.
