# ai-factory-status

Public BIGFIT Factory status / webmap (GitHub Pages).

- Live: https://liboriodsantos.github.io/ai-factory-status/
- Pointer: [`BRIDGE_STATE.md`](BRIDGE_STATE.md)
- Map: [`AI_FACTORY_MAP.md`](AI_FACTORY_MAP.md)
- Self-build: [`SELF_BUILD_STATUS.md`](SELF_BUILD_STATUS.md)

This repo is the public board, not the Factory software. Factory OS items **11–20** are recorded on the board (auth stub **PR #57**; Home cost **`UNAVAILABLE`** **PR #58**; `HTTP_BRIDGE` loopback on the standing host). `productionWrites` remains **false**; `FACTORY_MODE` remains **PAUSED**. Write class `factory.self_build.supervised` is named only — no expansion. Director keys stay **off host**. Ellisbrook live attach is **NO-GO**.
